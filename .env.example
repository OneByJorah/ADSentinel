# =============================================================================
# ADSentinel — Environment Configuration
# Copy to .env and fill in your values
# =============================================================================

# Flask environment
FLASK_ENV=production

# No additional environment variables required for basic operation.
# The app reads mock_dc_status.json at startup.
# For production, mount a real data source at /app/mock_dc_status.json
# or extend the app to read from an external API.
