# =============================================================================
# DirWatch — Environment Configuration
# Copy to .env and fill in your values
# =============================================================================

# Flask environment
FLASK_ENV=production

# Path to the JSON status file the dashboard reads.
# Mount/replace this with real collector output in production.
STATUS_FILE=/app/mock_dc_status.json
